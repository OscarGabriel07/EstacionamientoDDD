package org.example.estacionamiento.factura;

import co.com.sofka.domain.generic.Entity;
import co.com.sofka.domain.generic.Identity;

public class Ticket extends Entity {
    public Ticket(Identity entityId) {
        super(entityId);
    }
}
